<p>
    <%= link_to task.title,  collection_task_path(task) %>
  </p>

  <p><%= %></p>
  
  <p>
  <%= link_to "Destroy Task", [task.collection, task], data: {
                turbo_method: :delete,
                turbo_confirm: "Are you sure?"
              } %>
  </p>
<hr>

